import React from 'react'

export default function LightBtn(props) {

    const lightClasses = `light-btn btn-${props.bgClass}`

    return (
        <button type='button' className={lightClasses}>
            
        </button>
    )
}
